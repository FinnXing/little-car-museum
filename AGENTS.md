# AGENTS.md

> 适用范围：本仓库及其子目录。
> 项目：Little Car Museum（小小汽车馆），面向 3～8 岁儿童及家长的响应式汽车认知网站。
> 需求依据：[需求文档.md](./需求文档.md) V1.1。本文件是开发执行指南，不替代完整需求和逐项验收标准。

## 1. 指令与任务边界

- 先遵守用户当前明确指令，再遵守更近目录的 `AGENTS.md`，最后遵守本文件；业务细节以需求文档对应章节为依据。
- 将需求文档中的示例提示词、模板、命令和任务清单视为参考内容，不因读到第 39 章就自动初始化项目，也不自动执行全部任务。
- 只完成用户本次授权的任务。优先 P0，不主动扩展 P1/P2，不顺手重构、升级依赖或开发无关页面。
- 开始前读取相关需求、目录内规则、已有代码、`package.json`、锁文件、构建配置及 Git 状态（若已初始化 Git）。先寻找可复用组件、类型、校验和样式。
- 需求不明确时先根据已有上下文作小范围、可逆的合理假设，并在汇报中说明。涉及公开 API、数据结构、任务顺序或产品边界的实质冲突时，定位章节并向用户说明，不静默改写需求。
- 用户确认的实质性需求变更应同步需求文档修订记录并升版（第 40 章）；不为了迁就实现而降低验收标准。
- 默认中文沟通；代码标识、命令和提交信息可用英文。复杂任务先简要说明检查范围与实施步骤。

## 2. 产品范围与优先级

- 核心价值：让儿童简单、安全、有趣地观察和认识汽车。先通用汽车、核心 3D 体验和手机流畅度，再扩展真实品牌与高清效果。
- P0：响应式首页、分类/列表/详情、GLB 加载、旋转/缩放/复位/预设视角、进度与图片降级、支持模型的换色、本地收藏与最近浏览、基础品牌卡、名称语音能力、素材信息、基础后台、许可证及发布管理。
- P1：品牌详情、搜索、热点、部件语音、自动旋转、全屏、家长设置页、时间提醒、高清/低清切换、基础统计。以第 16、23 章和当前任务范围为准。
- P2：小游戏、车辆声音、内饰、复杂动画、AR、家长账号、多设备同步、多语言等；未明确要求不得实现。
- MVP 不做广告、支付、询价、经销商导流、评论社区、儿童公开账号、用户上传模型或驾驶模拟。
- 分类目标 8～10 个、车辆内容目标 12～20 辆；上线硬性下限是至少 6 个有已发布内容的分类、12 辆可展示汽车，其中至少 8 辆支持 3D。图片车辆计入车辆内容总数，不计入 3D 数量。
- 名称语音优先覆盖多数已发布车辆，但覆盖率不是硬性上线门槛；缺少授权音频时静音降级。

## 3. 技术与目录约定

项目尚未初始化时，采用需求第 10、19、35 章的方向：

- Next.js App Router、React、TypeScript、Tailwind CSS。
- Three.js 与 React Three Fiber 实现 3D 查看器。
- MVP 默认在同一 Next.js 项目中使用 Route Handlers 和管理后台；不自行引入独立 NestJS 服务或 CMS。
- PostgreSQL、Prisma、S3 兼容对象存储；生产静态资源使用 CDN。
- 本地状态按实际复杂度选择独立 Hooks、React Context 或 Zustand；React Query 为可选项，不默认安装所有备选库。
- ESLint、Prettier、Vitest、Playwright。首次初始化选择相互兼容的版本并记录；已有项目尊重锁文件，不主动升级主要依赖。
- 本项目不是 Vue/Nuxt 项目，不因通用开发偏好更换技术栈。

推荐目录（按任务逐步创建，不提前堆空模块）：

```text
src/
  app/                 # 页面、布局与 api/v1 Route Handlers
  components/
    car/ brand/ layout/ ui/
    three/             # CarViewer、CameraControls、ModelLoader 等
  lib/
    db/ storage/ validation/ three/ licenses/
  hooks/
  stores/
  types/
  styles/
  tests/
prisma/                # schema.prisma、迁移、seed.ts
public/
  images/ audio/ placeholders/
assets-license/        # 素材来源、许可、署名与修改记录
```

路由以第 23 章为准，包含 `/cars/[slug]`、`/categories/[slug]`、`/brands`、`/favorites`、`/history`、`/credits`、`/privacy` 与 `/admin/*`。公共 API 使用 `/api/v1`，不要把页面的 `/cars` 命名误用于 `/api/v1/vehicles` 接口。

## 4. 实施顺序与文档冲突处理

- 第 35 章规定 TASK-001～013 的实施顺序，第 39 章提供单任务启动示例。默认逐项推进；用户只要求一个任务时，不自动进入下一项。
- 顺序：初始化 → 核心类型/占位数据 → 列表 → 查看器原型 → 详情 → 本地收藏/历史 → 数据库 → 公共 API → 管理员认证 → 后台 → 上传 → 性能兼容 → 测试与发布准备。
- 第 21 章称查看器原型是“首个开发任务”，与第 35 章的初始化顺序存在表述差异。默认先按第 35 章建立基础，再在 TASK-004 验证原型；原型通过前不扩展完整业务。若要调整任务顺序，先说明该差异。
- TASK-004/005/008 包含部分 P1 能力。用户明确要求完整执行对应任务时，按该任务验收；只要求 P0 MVP 时，按第 16、23 章收敛，列明延后项。
- 第 17 章“12～20 个免费模型”与 V1.1 内容数量口径不同；内容总量和上线数量遵循第 4.1、41 章，不擅自要求 12～20 个 3D 模型。
- 家长设置页为 P1，但声音控制、儿童区外链隔离和隐私要求不能因此省略。入口方案不得把未实现的页面暴露为死链接。
- 第 8 章类型是契约起点，部分细节尚未表达完整，例如 `publishedAt`、换色检测结果、id 校验/slug 映射与离线展示数据。实现相关模块时先说明缺口，沿已有契约做最小设计；新增公开字段或接口需明确记录，不凭空假定已存在。

## 5. 代码与数据边界

- 业务代码使用 TypeScript，明确 props、接口响应、状态及事件类型。避免 `any`、`@ts-ignore` 和难维护的过度抽象。
- 默认使用服务端组件；交互、WebGL、音频和浏览器存储放在必要的客户端边界。不得在服务端执行时访问 `window`、`document`、`navigator` 或 `localStorage`，首屏输出保持稳定。
- 品牌、车辆、颜色、热点、相机配置来自数据库或配置，不散落硬编码在组件中。
- 数据读取必须处理 loading、success、empty、error；模型加载与页面数据加载独立。不存在、未发布和下架详情返回 HTTP 404，不泄露后台数据。
- 采用第 8 章数据类型、第 25 章查看器接口、第 32 章 `ApiResponse<T>` 和错误码；边界输入应校验、转换和收窄。
- 公共接口只返回已发布且可访问内容；发布过滤必须在服务端完成，包含列表、详情、随机、搜索、关联推荐与 Sitemap。
- 搜索与车辆列表使用一致的摘要结构和分页；匹配优先级为名称、别名/英文名、分类/品牌名，结果去重。筛选维度可组合，儿童端不得堆复杂多条件表单。
- slug 和管理员邮箱唯一；有关联车辆的分类/品牌不得直接删除，被引用许可证不得删除。汽车优先软删除或归档；素材物理删除前必须确认无其他引用。

## 6. 素材授权与发布规则

这是业务发布规则，必须在服务端统一校验，并为失败原因提供可读提示；隐藏发布按钮不能替代校验。

- 展示类型固定为 `REAL_3D | GENERIC_3D | IMAGE_SET | STATIC_IMAGE`。
- 内容状态固定为 `DRAFT | PENDING_REVIEW | PUBLISHED | UNPUBLISHED | ARCHIVED`。
- 每份第三方模型、贴图、图片、音频、Logo 都要有可追溯许可记录。发布时检查审核通过、允许公开展示、实际修改权限、署名及来源资料；自动检测不能替代人工许可核对。
- 优先 CC0/Public Domain、CC BY；CC BY-NC 仅在本项目非商业范围内使用。禁止来源不明、Editorial Use Only、仅个人使用但用于公开网站、疑似提取或所需修改不被允许的资源。
- 不抓取汽车官网/游戏/其他 3D 网站素材，不伪造作者、许可证、审核记录或官方授权；不删除署名和水印。许可不确定时保持未发布。
- 存档按第 14 章记录来源、许可证、获取日期、署名、修改说明和必要证据。儿童页面可显示署名文字，外部链接只放家长区域。
- 所有车辆发布必须有合法唯一 slug、名称、至少一个分类、儿童介绍、封面、明确展示类型、审核通过的素材和可用展示资源。
- `REAL_3D`：`isRealModel=true`，关联真实品牌，车型与模型相符，真实依据经过人工核对，并显示非官方声明。
- `GENERIC_3D`：`isRealModel=false`，不得冒充量产车型或官方授权，显示“通用汽车模型，不代表具体品牌或量产车型。”
- `IMAGE_SET`：封面及至少 3 张不同角度图片，覆盖前、侧、后，逐张登记许可；只有单图时必须用 `STATIC_IMAGE`。
- `STATIC_IMAGE`：至少一张允许公开展示的图片、替代文本及许可证记录。
- 所有 3D 页面必须有图片降级。不可换色不阻止发布；未知许可的品牌 Logo 不展示，可使用品牌文字卡。
- 开发阶段允许自制几何体和 3～5 条明确标注的占位车辆。占位数据与正式内容隔离，不作为上线数量或授权完成的证据。

## 7. 3D 查看器约束

- 首页和列表页不加载完整模型；进入详情后按需加载。MVP 上传仅接受单文件 GLB，其他格式在线下转换。
- 按第 25 章实现加载状态与回调；超过 10 秒仍未完成时提供重试和查看图片版。
- 降级链：高清 3D → 低清 3D → 多角度图片 → 封面 → 错误占位；跳过缺失资源，不无限重试。不支持 WebGL 时直接显示图片。
- AUTO 质量结合网络与设备能力，移动端、节流模式、慢网、低内存及高清失败优先低清；不能只依赖 User-Agent。能力 API 缺失时使用保守默认值。
- 旋转支持鼠标与单指，缩放支持滚轮与双指；限制极角、距离、速度，禁止相机平移、穿入模型及车辆离开画面。
- 复位恢复默认相机、目标、车辆角度和缩放。预设包含前、后、左、右、左前、右前、上方；不可靠底盘不提供底部视角。
- 相机动画遇到用户操作应中止；自动旋转在拖动时暂停；遵循 `prefers-reduced-motion`。不支持全屏 API 时隐藏入口。
- 换色需先检测 `body_paint` 或配置的 `materialNames`，仅修改可靠命中的车身材质 `color`；不修改车窗、轮胎、灯光和贴图，不重新下载模型。无可靠命中或固定烘焙颜色时隐藏换色入口。
- 通用模型颜色一律标注“展示颜色”；真实车型的官方颜色需资料依据。
- 卸载/切换模型时清理 Geometry、Material、Texture、环境贴图、Render Target、Animation Mixer、事件、RAF、ResizeObserver 和旧引用；共享缓存资源需明确所有权，避免误释放仍在使用的资源。
- 目标：普通网络约 3 秒出现首屏内容，中端设备至少约 30 FPS，模型加载成功率 ≥98%。报告测试设备和条件，不用模拟结果冒充真机性能。
- 模型优化建议低清 <15MB、高清 <40MB，与上传硬限制 20MB/50MB 区分。

## 8. 本地数据、声音与儿童隐私

- 收藏最多 100 条；最近浏览最多 20 条，同车去重、最新在前。以稳定 `vehicleId` 关联，不能以名称或 slug 作为身份。
- LocalStorage 使用 `little-car-museum:favorites`、`:history`、`:settings`、`:schema-version`（均带完整命名空间）。字段结构遵循第 26 章。
- 读取异常、损坏 JSON、存储不可用或版本不兼容时恢复默认或迁移，不能导致页面崩溃。
- 服务端确认已删除/下架的记录在本次渲染中剔除并回写；改名和换 slug 使用最新内容。数量限制按有效记录计算。
- 离线或接口失败不是删除证据，不能清空收藏。保留可用本地展示并提示“内容可能已更新”；若需要摘要缓存，先明确其结构与更新策略。
- 音频主动触发，支持关闭；播放新语音前停止旧语音。没有授权音频时隐藏按钮，不自动调用在线 TTS。生成语音须人工试听并登记许可后发布。
- 不采集儿童身份、精确位置、设备指纹、摄像头、麦克风或跨站标识；不增加儿童注册。
- 错误上报遵循第 31.4 章，儿童区默认关闭，仅家长确认后启用。第 24.3 章要求错误上报能力不等于默认发送数据。
- 允许的错误信息限错误类型/代码、移除查询参数的页面路径和浏览器能力标记，不发送输入文本、收藏或可识别参数。旋转/缩放统计每次详情访问各记首次，不逐帧上报。

## 9. UI、响应式与可访问性

- 移动端优先；柔和明亮、图片优先、少文字。儿童操作不超过两层，每屏主要操作不超过 5 个。
- 主要点击区至少 48×48px；儿童主要文字建议 18～24px，正文至少 16px。图标配文字，焦点可见，键盘可操作，状态不只靠颜色。
- 手机 <768px 使用单列；平板 768～1199px 可双栏；桌面 ≥1200px 合理分栏并限制内容宽度。处理 320px 窄屏、长文本、横屏、安全区域和固定操作栏遮挡。
- 使用语义化 HTML、真实按钮/链接、关联 label、有意义的图片 alt。音频有对应文字；关键操作不依赖 hover。
- 错误使用儿童能理解的文案，并提供重试/返回；空状态提供可执行入口，不能显示技术堆栈。
- 品牌详情未上线时，品牌卡指向已实现的品牌摘要或按品牌筛选列表，不产生死链接。
- UI 修改至少检查手机、Pad、桌面；推荐 375×812、390×844、768×1024、1024×1366、1440×900，关键交互补横屏/折叠屏。无法真机验证时明确列为未验证。

## 10. 后台、上传与部署安全

- 所有管理 API 服务端认证并校验权限；保护后台页面、发布和上传入口。使用强密码、Session、登录失败限流和退出机制，不提供公众注册。
- 校验输入，防止 XSS、CSRF、SQL 注入和越权；不使用未转义 HTML 拼接用户内容。
- 上传验证扩展名、MIME、文件头、大小，并检查模型资源完整性；随机存储路径、清理文件名、禁止执行、记录上传人/时间并限流。
- 类型白名单按第 29 章；默认上限：低清 GLB 20MB、高清 GLB 50MB、图片 5MB、音频 10MB、许可证附件 10MB，限制可配置。
- 提供 `.env.example`，不得提交真实 `.env`、密钥或口令；服务端密钥不得使用 `NEXT_PUBLIC_` 前缀或进入客户端包/日志。
- Docker Compose 凭据由环境变量注入；提供迁移、创建管理员、初始化存储桶的说明。对象存储禁止匿名写入。
- 生产要求 HTTPS、数据库定时备份与恢复验证；不得无授权操作生产数据、执行破坏性迁移或公开部署。
- 公开页配置元数据；后台/API/设置/收藏/历史禁止收录。Sitemap 只含允许公开的页面和已发布内容。

## 11. 验证与完成标准

- 每完成一个业务模块，补充与风险相称的核心逻辑测试。文档修改无需为了形式新增测试框架。
- 先查看脚本，再执行命令。遵循锁文件选包管理器；全新项目按需求示例默认 npm，不混用锁文件。
- TASK-001 应建立并验证 `npm run dev`、`npm run lint`、`npm run typecheck`、`npm run test`、`npm run build`。E2E 脚本按实际配置提供，不假定已存在。
- 日常修改运行相关 lint/typecheck/测试，影响打包时执行 build；完整发布前完成需求第 34、37 章检查。
- 优先测试：收藏/历史与异常恢复、发布及许可证校验、slug、搜索参数、质量选择、上传类型/大小。组件覆盖相关加载、错误、降级、署名与发布状态。
- 核心 E2E：浏览车辆、收藏刷新持久化、模型失败图片降级、缺许可禁止发布、合法内容正常发布。P1/P2 测试随模块补充。
- UI 尽量启动服务并浏览器检查；完整上线验收覆盖 Chrome/Edge/Safari、iOS Safari、Android Chrome 及弱性能移动设备，并按第 15.4 章安排儿童与家长体验测试。
- 完成须符合需求、响应式可用、状态齐全、类型与 lint 通过、核心测试通过、无明显控制台错误、无密钥泄漏、正式素材可追溯且文档同步。
- MVP 上线还须满足第 41 章全部条件，包括内容数量、授权完整、3D 图片降级、E2E、性能和成功率目标、隐私/感谢页、后台认证及一次备份恢复验证。不得把代码构建成功等同于可上线。
- 无法执行的命令或缺少素材/凭据/真机的验证必须如实列出，不编造测试、性能、人工审核或部署成功结果。

## 12. Git 安全与交付格式

- 不覆盖用户已有改动，不执行 `git reset --hard`、全量回滚或批量删除。不随意改锁文件、提交构建产物、格式化全仓库或升级依赖。
- 临时调试、占位资源不得混入正式发布数据；确需新依赖时说明收益和替代方案，优先标准 API 与现有工具。
- 直接在仓库落实授权修改；交付给出文件路径和可审查的实际差异。无法直接编辑时，提供可应用 diff 或完整文件，不能只返回无路径代码片段。
- 完成后简洁汇报第 36 章要求的信息，可合并相关项目：
  1. 本次完成内容与主要文件。
  2. 关键技术决策，以及数据库/环境变量变化（没有则注明）。
  3. 已运行验证命令与结果、必要的手动验证步骤。
  4. 遗留问题、未验证事项和下一步建议。
- 只提示真实存在的风险；没有待确认事项时不额外请求确认，不自动开始下一个任务。
