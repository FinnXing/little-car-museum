# 儿童 3D 汽车认知网站需求文档

> 文档版本：V1.1
> 项目代号：Little Car Museum
> 产品名称：小小汽车馆（暂定）
> 产品形态：响应式 Web 网站
> 目标用户：3～8 岁儿童及其家长
> 项目性质：家庭使用或非商业学习项目
> 更新日期：2026-08-11

## 修订记录

| 版本 | 日期 | 修订内容 |
|---|---|---|
| V1.0 | 2026-08-11 | 初版需求文档。 |
| V1.1 | 2026-08-11 | 需求评审修订：①统一“分类数 / 模型数 / 可展示车数”口径（4.1、5、41）；②补充语音来源方案与 P0/P1 取舍（6.7）；③明确本地收藏与浏览记录在内容变更后的失效策略（26.6）；④补充可换色判定与降级规则（FR-3D-006）；⑤将 IMAGE_SET 多角度图设为硬性发布条件（28.4）；⑥补充需求变更记录；⑦明确错误上报匿名化（31）；⑧补充搜索返回结构、tags/别名、FOV 覆盖等若干说明。 |

> 需求变更遵循第 40 章“需求变更原则”。任何实质性变更须在本表追加一行并升版。

---

# 1. 项目概述

## 1.1 项目背景

本项目面向喜欢汽车的儿童，提供简单、安全、有趣的汽车 3D 浏览体验。

儿童可以通过鼠标或触摸操作旋转汽车，从前、后、左、右、上方观察汽车，并认识不同汽车类型、外观部件、颜色及品牌。

由于项目暂无商业素材采购预算，首期优先使用授权明确的免费 3D 模型、图片和音频资源。

## 1.2 核心目标

1. 提供汽车 3D 旋转、缩放、复位和视角切换功能；
2. 通过图片、文字和语音帮助儿童认识汽车；
3. 支持轿车、SUV、跑车、越野车、皮卡等分类；
4. 在授权条件允许时展示真实品牌和车型；
5. 建立后台管理能力，方便持续增加汽车内容；
6. 保证儿童使用安全，不展示广告和不适宜内容；
7. 保证所有第三方素材都有明确来源和许可证记录。

## 1.3 产品定位

在没有商业授权预算的情况下，首期产品定位为：

> 面向儿童的 3D 汽车认知网站，以开放授权的通用汽车模型为主，在授权明确时补充真实品牌和车型内容。

首期不承诺覆盖所有汽车品牌，也不承诺所有热门车型都有真实 3D 模型。

---

# 2. 核心约束

## 2.1 免费素材约束

“网上可以免费下载”不等于“可以在网站中公开使用”。

所有第三方资源必须满足以下条件：

1. 有明确的原始来源页面；
2. 有明确的许可证；
3. 许可证允许网页公开展示；
4. 如果需要格式转换、减面、修改颜色或材质，许可证必须允许修改；
5. 按照许可证要求进行作者署名；
6. 保存来源页面、许可证和获取日期；
7. 不使用来源不明的网盘资源；
8. 不从汽车官网、汽车游戏或其他 3D 网站中提取模型；
9. 不删除素材水印和作者信息；
10. 不使用仅限个人使用的资源建设公开网站。

## 2.2 可接受的许可证

优先级如下：

1. CC0 / Public Domain；
2. CC BY；
3. 明确允许非商业展示的 CC BY-NC；
4. 其他明确允许本项目使用的开放许可证。

以下资源禁止使用：

- 没有许可证说明；
- Personal Use Only，但网站需要公开访问；
- Editorial Use Only；
- 不允许修改，但项目需要转换格式；
- 疑似从游戏或其他网站提取；
- 作者、来源或贴图授权不明确；
- 需要署名但无法提供正确署名信息。

## 2.3 商业化限制

使用带有 `NC` 限制的资源时，网站不得加入：

- 广告；
- 会员收费；
- 品牌赞助；
- 商品销售；
- 导购佣金；
- 付费课程；
- 付费下载。

如果以后商业化，必须重新审核并替换不允许商业使用的素材。

---

# 3. 用户角色

## 3.1 儿童用户

年龄范围：3～8 岁。

主要需求：

- 找到喜欢的汽车；
- 用鼠标或手指旋转汽车；
- 观察汽车前后左右；
- 更换汽车颜色；
- 听到汽车名称；
- 认识车灯、车门和车轮等部件；
- 收藏喜欢的汽车。

设计要求：

- 大按钮；
- 少文字；
- 图片优先；
- 操作层级不超过两层；
- 不出现广告；
- 不出现可直接跳转的外部链接；
- 不要求注册。

## 3.2 家长用户

主要需求：

- 确保内容安全；
- 控制声音；
- 查看素材来源；
- 设置使用时间提醒；
- 清除收藏和浏览记录；
- 帮助孩子搜索汽车。

## 3.3 管理员

主要需求：

- 管理汽车分类；
- 管理品牌；
- 管理车型；
- 上传和预览 3D 模型；
- 配置汽车颜色和部件热点；
- 管理素材许可证；
- 发布、下架和排序内容；
- 查看模型加载情况。

---

# 4. 产品范围

## 4.1 MVP 内容规模

首个可用版本（即 41 章“MVP 上线判定”）包含：

- 汽车分类：4.2 定义了 10 个候选分类；首期 MVP 目标开放 8~10 个（41 章要求至少 6 个已发布分类），新增/停用可通过后台动态管理；
- 12～20 辆**可展示汽车**（以下统称“车辆内容”，含 3D、多角度图、单图三种展示方式）；
  - 其中 **12～20 辆**中至少 **8 辆**具备开放授权的 3D 模型（3D 车辆数 + 图片降级车辆数 = 车辆内容总数）；
  - 没有 3D 模型的车辆通过 `IMAGE_SET` 或 `STATIC_IMAGE` 展示，仍计入“可展示汽车”；
- 每辆汽车提供 1～5 种颜色；
- 每辆汽车提供基础介绍；
- 每辆汽车可配置 0～5 个部件热点；
- 10～20 个汽车品牌文字认知卡；
- 本地收藏和最近浏览；
- 基础内容管理后台。

> 数字关系说明：`车辆内容总数 = 拥有 3D 模型的车辆数（≥8）+ 仅图片展示的车辆数`。全文出现的“车辆内容 / 可展示汽车 / 3D 车辆”按此口径统一理解。

## 4.2 汽车分类

首期建议包含：

- 轿车；
- SUV；
- 跑车；
- 越野车；
- MPV；
- 皮卡；
- 新能源车；
- 老爷车；
- 工程车辆；
- 特种车辆。

## 4.3 展示类型

每条汽车内容必须设置展示类型：

### REAL_3D

真实车型 3D。

使用条件：

- 模型来源明确；
- 允许公开展示；
- 允许必要修改；
- 车型名称与模型相符；
- 已完成版权及商标风险核对。

### GENERIC_3D

通用汽车 3D。

页面必须标注：

> 通用汽车模型，不代表具体品牌或量产车型。

### IMAGE_SET

多角度图片展示。

适用于没有合适 3D 模型的品牌或车型。

### STATIC_IMAGE

单张图片和文字介绍。

作为最低级降级方案。

## 4.4 首期不实现

- 汽车购买和询价；
- 经销商导流；
- 用户评论和社区；
- 儿童公开账号；
- 在线支付；
- 用户上传模型；
- 真实驾驶模拟；
- 复杂机械结构教学；
- VR；
- AR；
- 车门和发动机等复杂动画；
- 真实车辆价格；
- 实时销量数据。

---

# 5. 信息架构

```text
首页
├── 3D 看汽车
├── 热门汽车
├── 按类型找汽车
├── 认识汽车品牌
├── 随机看一辆
├── 我的收藏
└── 最近浏览

汽车分类
├── 轿车
├── SUV
├── 跑车
├── 越野车
├── MPV
├── 皮卡
├── 新能源车
├── 老爷车
├── 工程车辆
└── 特种车辆

汽车详情
├── 3D 展示
├── 预设视角
├── 颜色切换
├── 汽车信息
├── 部件热点
├── 名称语音
├── 收藏
└── 素材信息

品牌馆
├── 品牌列表
└── 品牌详情

家长设置
├── 声音设置
├── 自动旋转设置
├── 使用时间提醒
├── 清除收藏
├── 清除浏览记录
├── 隐私说明
└── 素材与许可证

管理后台
├── 分类管理
├── 品牌管理
├── 汽车管理
├── 3D 模型管理
├── 素材授权管理
└── 发布管理
```

---

# 6. 功能需求

## 6.1 首页

### FR-HOME-001 热门汽车

系统展示管理员设置为热门的汽车。

验收标准：

- 展示汽车封面、名称和类型；
- 点击卡片进入汽车详情；
- 未配置热门汽车时，按最新发布（`publishedAt` / `updatedAt`）倒序展示，数量不超过热门区的固定条数；
- 热门区只展示已发布、可正常访问的汽车（含仅图片降级的车辆）；
- 首页不直接加载 3D 模型文件。

### FR-HOME-002 汽车分类入口

系统展示汽车分类图标和名称。

验收标准：

- 点击后进入对应分类列表；
- 分类可在后台排序；
- 没有已发布内容的分类不显示。

### FR-HOME-003 随机看一辆

点击后随机打开一辆已发布且可正常展示的汽车。

### FR-HOME-004 收藏和最近浏览

首页可进入收藏列表和最近浏览列表。

---

## 6.2 汽车列表

### FR-LIST-001 汽车卡片

每张卡片展示：

- 封面图；
- 汽车名称；
- 汽车类型；
- 展示类型；
- 收藏状态。

### FR-LIST-002 筛选

MVP 支持：

- 按汽车分类筛选；
- 按展示类型筛选；
- 按品牌筛选。

交互规则：

- 三个维度支持**组合叠加**（如：某分类 + 某展示类型）；
- 为符合“每屏主要操作不超过 5 个”（11.2）与“操作层级不超过两层”（3.1），儿童端一次最多启用一个维度或拖动式简单叠加，禁止弹出多条件复杂表单；品牌筛选在后台按字母/热门降序定位；
- 已选条件的清除结果互不影响（分别清除），筛选为空时展示空状态（24.2）。

### FR-LIST-003 搜索

支持搜索：

- 汽车中文名称；
- 汽车英文名称；
- 品牌中文名称；
- 品牌英文名称；
- 别名；
- 汽车分类。

规则：

- 搜索按最优匹配排序，优先命中“名称”，其次“别名/英文名”，再次“分类/品牌名”；
- 搜索结果按车辆的可展示状态过滤（只返回已发布内容）；
- 同一查询可同时命中名称、别名与分类时，结果去重；
- 没有搜索结果时推荐热门汽车。

> 搜索接口返回结构见 9.1，返回数据与汽车列表接口（GET /api/v1/vehicles）字段一致。

---

## 6.3 汽车 3D 展示

### FR-3D-001 旋转

电脑端：

- 按住鼠标左键拖动旋转；
- 水平方向支持 360° 旋转；
- 垂直方向限制在合理范围内。

触摸端：

- 单指拖动旋转；
- 不影响页面必要的返回操作。

建议垂直角度限制：

```text
minimumPolarAngle: 15°
maximumPolarAngle: 100°
```

具体值允许通过汽车配置调整。

### FR-3D-002 缩放

电脑端使用鼠标滚轮缩放。

移动端使用双指缩放。

必须限制最小和最大距离，防止：

- 摄像机进入模型内部；
- 汽车完全离开画面；
- 汽车过小而无法观察。

### FR-3D-003 复位

点击复位按钮后恢复：

- 默认相机位置；
- 默认观察目标；
- 默认汽车旋转角度；
- 默认缩放距离。

双击 3D 区域也可以触发复位。

### FR-3D-004 预设视角

提供以下按钮：

- 前；
- 后；
- 左；
- 右；
- 左前；
- 右前；
- 上方。

点击后相机平滑移动到目标位置。

如果模型不包含可靠底盘，不提供底部视角。

### FR-3D-005 自动旋转

支持开始和暂停自动旋转。

要求：

- 用户主动拖动时暂停；
- 用户停止操作一段时间后是否继续旋转由设置决定；
- 尊重系统的“减少动态效果”设置。

### FR-3D-006 颜色切换

当模型支持车身材质切换时，展示颜色按钮。

要求：

- 只修改车身材质；
- 不修改车窗、轮胎和灯光；
- 切换颜色不重新下载完整模型、不重新加载页面；
- 不支持换色的模型隐藏颜色按钮；
- 非官方颜色标记为“展示颜色”，并在界面以“展示颜色”小标签区别于“官方颜色”（官方颜色仅用于真实车型、需依据真实车漆资料；通用模型一律视为“展示颜色”）。

**可换色判定规则**：

1. 按 10.3 的约定，可换色的模型须存在命名统一的车漆材质（统一材质名 `body_paint`，或后台按车辆配置的 `materialNames` 命中列表）。
2. 后台上传 / 预览模型时，按 `materialNames` 对模型材质进行**匹配检测**，并记录“是否支持换色”。前台依据该结果决定是否展示颜色按钮。
3. 当模型本身为单材质 / 整体烘焙、无法可靠区分车身，或 `materialNames` 未命中任何材质时，判定为**不可换色**：隐藏颜色按钮，且按 28.1 的发布规则该车辆仍可正常发布（换色是增强功能，不是发布必要条件）。
4. 换色仅在所有命中材质的 `color` 属性上实时更新，不得改动 `map`、`normalMap` 等会影响光照与法线的贴图；若某命中材质无 `color` 属性或为固定贴图色，该颜色项应在审查时标记为不可用。

### FR-3D-007 全屏

支持进入和退出全屏。

不支持全屏 API 时隐藏该按钮。

### FR-3D-008 加载状态

加载过程中显示：

- 汽车封面或轮廓图；
- 加载进度；
- “汽车正在开过来……”提示。

超过 10 秒仍未完成时显示：

- 重新加载；
- 查看图片版。

### FR-3D-009 加载失败降级

降级顺序：

```text
高清 3D
→ 低清 3D
→ 多角度图片
→ 单张封面图
→ 错误占位图
```

### FR-3D-010 部件热点

可配置热点：

- 车灯；
- 车轮；
- 车门；
- 后视镜；
- 发动机盖；
- 后备箱；
- 充电口；
- 加油口。

点击热点后：

- 高亮或聚焦对应部位；
- 显示部件名称；
- 显示儿童版解释；
- 可播放名称语音。

---

## 6.4 汽车信息

每辆汽车可以配置：

- 中文名称；
- 英文名称；
- 儿童化名称；
- 所属品牌；
- 汽车分类；
- 能源类型；
- 座位数量；
- 一句话介绍；
- 展示类型；
- 是否为真实车型；
- 封面图；
- 名称语音。

儿童介绍控制在 20～80 个汉字。

通用模型介绍示例：

> 这是一辆红色跑车。它的车身比较低，看起来像正在准备向前冲。

不得将通用模型描述为真实品牌量产车型。

---

## 6.5 品牌馆

### FR-BRAND-001 品牌卡片

展示：

- 品牌中文名称；
- 品牌英文名称；
- 国家或地区；
- 品牌 Logo，可选；
- 简短介绍。

如果车标使用条件不明确，只展示文字名称。

**入口降级**：品牌详情页（`/brands/[slug]`）按路由清单为 P1。在品牌详情未上线的 MVP 阶段，首页“认识汽车品牌”入口与品牌卡片可先指向品牌列表（P0），列表点击品牌后仅展示该品牌信息摘要或下拉到对应汽车列表（`?brand=`），不因详情页缺失而让入口失效。品牌详情上线后，品牌卡片再跳转详情。

### FR-BRAND-002 品牌详情

展示：

- 品牌名称；
- 国家或地区；
- 儿童版简介；
- 名称语音；
- 相关汽车内容；
- 非官方网站声明。

品牌简介建议控制在 30～80 个汉字。

---

## 6.6 收藏与浏览记录

### FR-FAVORITE-001 本地收藏

MVP 不要求注册，收藏保存在浏览器本地。

支持：

- 添加收藏；
- 取消收藏；
- 查看收藏列表；
- 清空收藏。

### FR-HISTORY-001 最近浏览

保存最近浏览的 20 辆汽车。

同一汽车只保留最新记录。

不保存儿童身份信息。

---

## 6.7 语音

支持播放：

- 品牌名称；
- 汽车名称；
- 汽车部件名称。

要求：

- 支持关闭声音；
- 新语音播放前停止上一段语音；
- 避免多段音频同时播放；
- 音频加载失败不影响页面使用；
- 首期优先使用自行录制或授权明确的音频。

### 6.7.1 语音来源方案

MVP 阶段不承诺全量素材都有高质量语音，采用**按优先级选择**的来源，缺音频时页面正常显示、仅静音跳过：

1. **人工录制**：自行录制或使用家庭设备录制的简短中文童声，许可证归本项目，用于高优先级关键词（品牌名、常见车名、常用部件如“车灯/车轮/车门”）。
2. **授权明确的开源 TTS / 音频库**：仅使用许可证允许公开展示的语音资源，并留存许可证记录（见 14 章素材管理）。
3. **降级静音**：某条内容没有授权语音时，隐藏该条目的“播放语音”按钮，不得回退到行为未知或涉及数据外发的在线 TTS 接口。

### 6.7.2 语音生成要求

若后续引入 TTS 生成音频（例如为批量补充汽车名称），必须满足：

- 使用本项目授权范围内的模型或服务，且不向任何第三方上传儿童名称等自拟文本以外的内容；
- 中文发音准确，专用名词（如“SUV”“MPV”）读音可人工校对；
- 生成的音频文件需人工试听后入库，视为正式素材（按 14 章登记许可证与生成时间），不得未经审核直接发布。

### 6.7.3 P0 / P1 取舍

- **P0**：至少保证“汽车名称”与“品牌名称”中，能对多数已发布车辆提供可播放的名称语音；其余语音缺口以静音降级兜底，不影响上线判定（见 41 章）。
- **P1**：补齐部件名称语音、自动旋转等（见 16.2）。
- 名称语音的可用率作为内容生产的度量项，但不作为 MVP 的硬性上线门槛。

---

## 6.8 家长设置

支持：

- 开启或关闭语音；
- 开启或关闭自动旋转；
- 设置 15、30、45 分钟使用提醒；
- 清除收藏；
- 清除浏览记录；
- 查看素材与许可证；
- 查看隐私说明。

外部链接必须放在家长区域。

---

# 7. 后台管理需求

## 7.1 分类管理

支持：

- 新增和编辑分类；
- 分类图标；
- 中英文名称；
- 排序；
- 启用和停用；
- 查看分类下汽车数量。

## 7.2 品牌管理

字段包括：

- 中文名称；
- 英文名称；
- 别名；
- 国家或地区；
- Logo；
- 儿童版简介；
- 名称语音；
- 是否热门；
- 排序值；
- 发布状态；
- 素材授权信息。

## 7.3 汽车管理

支持：

- 新增和编辑汽车；
- 关联品牌；
- 关联分类；
- 设置展示类型；
- 上传封面；
- 上传高清和低清模型；
- 配置图片降级资源；
- 配置默认相机；
- 配置颜色；
- 配置热点；
- 配置名称语音；
- 设置热门和排序；
- 预览；
- 发布和下架。

## 7.4 发布状态

统一状态：

```text
DRAFT
PENDING_REVIEW
PUBLISHED
UNPUBLISHED
ARCHIVED
```

只有 `PUBLISHED` 状态可以被普通用户访问。

## 7.5 模型检查

上传模型时检查：

- 文件扩展名；
- MIME 类型；
- 文件大小；
- 模型格式；
- 面数；
- 材质数量；
- 纹理尺寸；
- 外部资源是否丢失；
- 模型中心是否异常；
- 模型尺寸是否异常。

后台必须可以预览模型。

## 7.6 发布校验

以下信息缺失时禁止发布第三方素材：

- 作者；
- 来源详情页；
- 许可证；
- 许可证链接或本地副本；
- 是否允许公开展示；
- 是否允许修改；
- 署名文本；
- 获取日期。

---

# 8. 数据模型

## 8.1 Category

```typescript
interface Category {
  id: string;
  slug: string;
  nameCn: string;
  nameEn?: string;
  iconUrl?: string;
  description?: string;
  sortOrder: number;
  enabled: boolean;
  createdAt: string;
  updatedAt: string;
}
```

## 8.2 Brand

```typescript
interface Brand {
  id: string;
  slug: string;
  nameCn: string;
  nameEn?: string;
  aliases: string[];
  countryRegion?: string;
  logoUrl?: string;
  descriptionChild?: string;
  pronunciationAudioUrl?: string;
  isHot: boolean;
  sortOrder: number;
  status: ContentStatus;
  assetLicenseId?: string;
  createdAt: string;
  updatedAt: string;
}
```

## 8.3 Vehicle

```typescript
type DisplayType =
  | "REAL_3D"
  | "GENERIC_3D"
  | "IMAGE_SET"
  | "STATIC_IMAGE";

type ContentStatus =
  | "DRAFT"
  | "PENDING_REVIEW"
  | "PUBLISHED"
  | "UNPUBLISHED"
  | "ARCHIVED";

interface Vehicle {
  id: string;
  slug: string;
  brandId?: string;
  categoryIds: string[];
  nameCn: string;
  nameEn?: string;
  childName?: string;
  aliases: string[];
  /** 可选：标签（如“新能源”“卡车”“警车”），用于分类以外的模糊检索与专题（P2）预留；MVP 可不填 */
  tags?: string[];
  displayType: DisplayType;
  isRealModel: boolean;
  energyType?: "FUEL" | "ELECTRIC" | "HYBRID" | "OTHER";
  seatCount?: number;
  childDescription: string;
  coverImageUrl: string;
  thumbnailUrl?: string;
  highModelUrl?: string;
  lowModelUrl?: string;
  fallbackImageUrls: string[];
  pronunciationAudioUrl?: string;
  defaultCamera?: CameraConfig;
  colors: VehicleColor[];
  hotspots: Hotspot[];
  isHot: boolean;
  sortOrder: number;
  status: ContentStatus;
  assetLicenseIds: string[];
  createdAt: string;
  updatedAt: string;
}
```

## 8.4 CameraConfig

```typescript
interface Vector3 {
  x: number;
  y: number;
  z: number;
}

interface CameraConfig {
  position: Vector3;
  target: Vector3;
  minDistance: number;
  maxDistance: number;
  minPolarAngle: number;
  maxPolarAngle: number;
  /** 可选：覆盖默认 FOV（默认 35~50），不填则由前端使用通用默认值 */
  fov?: number;
}
```

## 8.5 VehicleColor

```typescript
interface VehicleColor {
  id: string;
  nameCn: string;
  colorValue: string;
  materialNames: string[];
  isOfficialColor: boolean;
  sortOrder: number;
}
```

## 8.6 Hotspot

```typescript
interface Hotspot {
  id: string;
  partName: string;
  position: Vector3;
  descriptionChild?: string;
  audioUrl?: string;
  cameraPosition?: Vector3;
  cameraTarget?: Vector3;
  enabled: boolean;
}
```

## 8.7 AssetLicense

```typescript
type AssetType = "MODEL" | "IMAGE" | "TEXTURE" | "AUDIO" | "LOGO";

interface AssetLicense {
  id: string;
  assetName: string;
  assetType: AssetType;
  authorName: string;
  sourcePageUrl: string;
  originalDownloadUrl?: string;
  licenseName: string;
  licenseUrl?: string;
  licenseFileUrl?: string;
  acquiredAt: string;
  allowPublicDisplay: boolean;
  allowModification: boolean;
  allowCommercialUse: boolean;
  attributionRequired: boolean;
  attributionText?: string;
  containsTrademark: boolean;
  modifications?: string;
  sourceScreenshotUrl?: string;
  reviewStatus: "PENDING" | "APPROVED" | "REJECTED";
  createdAt: string;
  updatedAt: string;
}
```

---

# 9. API 需求

API 路径统一使用 `/api/v1`。

## 9.1 公共接口

```text
GET /api/v1/categories
GET /api/v1/brands
GET /api/v1/brands/:slug
GET /api/v1/vehicles
GET /api/v1/vehicles/random
GET /api/v1/vehicles/:slug
GET /api/v1/search?q=
GET /api/v1/licenses/public
```

汽车列表接口支持：

```text
category
brand
displayType
keyword
isHot
page
pageSize
sort
```

> `GET /api/v1/search?q=` 返回结构与汽车列表接口（`GET /api/v1/vehicles`）的列表项一致，均为 Vehicle 摘要（含封面、名称、类型、展示类型、收藏态所需标识）；支持 `page/pageSize` 与按相关性排序，见 FR-LIST-003。

公共接口只返回已发布内容。

## 9.2 管理接口

```text
POST   /api/v1/admin/categories
PATCH  /api/v1/admin/categories/:id
DELETE /api/v1/admin/categories/:id

POST   /api/v1/admin/brands
PATCH  /api/v1/admin/brands/:id
DELETE /api/v1/admin/brands/:id

POST   /api/v1/admin/vehicles
PATCH  /api/v1/admin/vehicles/:id
DELETE /api/v1/admin/vehicles/:id
POST   /api/v1/admin/vehicles/:id/publish
POST   /api/v1/admin/vehicles/:id/unpublish

POST   /api/v1/admin/assets/upload
POST   /api/v1/admin/licenses
PATCH  /api/v1/admin/licenses/:id
```

所有管理接口必须进行身份认证和权限校验。

---

# 10. 技术方案

## 10.1 推荐技术栈

前端：

- Next.js；
- React；
- TypeScript；
- Tailwind CSS；
- React Three Fiber；
- Three.js；
- Zustand 或 React Context；
- React Query，可选。

后端：

- Next.js Route Handlers 或 NestJS；
- PostgreSQL；
- Prisma ORM；
- 对象存储；
- CDN。

管理后台：

- MVP 可使用同一个 Next.js 项目；
- 后续可以采用 Payload CMS、Directus 或独立后台。

## 10.2 3D 格式

统一使用：

- glTF 2.0；
- GLB。

优化方式：

- Meshopt 或 Draco；
- KTX2/Basis 纹理；
- 模型减面；
- 纹理压缩；
- 高清和低清两套模型。

## 10.3 3D 资源规范

建议规范：

- Y 轴朝上；
- 所有车辆车头方向一致；
- 模型中心位于坐标原点附近；
- 模型落在地面平面上；
- 移动端模型建议小于 15MB；
- 桌面高清模型建议小于 40MB；
- 普通纹理优先使用 1024×1024；
- 重点纹理最高使用 2048×2048；
- 材质数量尽量不超过 10 个；
- 车漆材质统一命名为 `body_paint`；
- 车窗、轮胎、车灯使用独立材质。

## 10.4 模型处理流程

```text
确认许可证
→ 保存授权资料
→ 下载原始模型
→ 使用 Blender 检查
→ 删除无关摄像机和灯光
→ 检查贴图
→ 统一比例、朝向和原点
→ 减少面数
→ 整理材质
→ 压缩纹理
→ 导出 GLB
→ 生成高清和低清版本
→ 多设备测试
→ 上传后台
→ 管理员审核
→ 发布
```

---

# 11. UI/UX 要求

## 11.1 视觉原则

- 使用明亮、柔和的儿童友好配色；
- 以汽车为页面视觉中心；
- 减少文字；
- 使用大按钮和圆角卡片；
- 图标与文字同时使用；
- 不展示广告；
- 不使用诱导点击的设计。

## 11.2 尺寸要求

- 主要点击区域不小于 48×48px；
- 儿童主要文字建议 18～24px；
- 普通正文不小于 16px；
- 每屏主要操作不超过 5 个；
- 文字颜色满足基本对比度要求。

## 11.3 响应式断点

```text
手机：小于 768px
平板：768px～1199px
桌面：1200px 及以上
```

3D 区域建议：

- 手机占首屏高度 55%～65%；
- 平板和桌面占主要内容区域 65%～75%。

**汽车详情页布局（结合 3.1“操作层级≤两层”）**：

- 手机：3D 区在上、信息/操作在下，单列堆叠，最多两层导航；
- 平板（768~1199px）：3D 区与汽车信息可采用左右分栏，但保证主要操作按钮同时可见，不支持多级折叠子菜单；
- 桌面（≥1200px）：可分栏展示 3D + 信息 + 素材信息，控件走查保持每屏主要操作≤5 个。

## 11.4 无障碍

- 图片必须设置替代文本；
- 主要按钮支持键盘操作；
- 控件有焦点状态；
- 音频内容有对应文字；
- 不仅依靠颜色表示状态；
- 支持 `prefers-reduced-motion`；
- 使用语义化 HTML。

---

# 12. 性能要求

## 12.1 页面性能

目标：

- 普通网络下首屏可见内容在 3 秒左右出现；
- 不在汽车列表页加载完整 3D 模型；
- 进入详情页后再加载模型；
- 图片使用 WebP 或 AVIF；
- 静态资源通过 CDN 分发；
- 模型和图片使用懒加载。

## 12.2 3D 性能

目标：

- 中端设备不低于 30 FPS；
- 优先达到 45～60 FPS；
- 模型加载成功率不低于 98%；
- 模型加载失败时必须降级；
- 页面离开后释放 Three.js 场景、纹理和材质。

## 12.3 设备能力判断

根据设备性能选择：

```text
高性能设备 → 高清模型
普通设备 → 低清模型
不支持 WebGL → 图片展示
```

---

# 13. 安全与隐私

## 13.1 儿童隐私

MVP 不收集：

- 儿童姓名；
- 生日；
- 照片；
- 学校；
- 精确位置；
- 联系方式；
- 社交关系。

收藏和最近浏览默认保存在本地。

## 13.2 网站安全

要求：

- 全站 HTTPS；
- 后台启用强密码；
- 管理员权限控制；
- 文件上传检查扩展名、MIME 和大小；
- 防止 XSS、CSRF 和 SQL 注入；
- 数据库定期备份；
- 对象存储禁止匿名写入；
- 不在前端暴露服务密钥；
- 上传文件使用随机文件名。

---

# 14. 素材管理与署名

## 14.1 素材存档目录

每份资源建议按以下格式保存：

```text
assets-license/
└── asset-id/
    ├── source.txt
    ├── license.txt
    ├── source-page.png
    ├── attribution.txt
    ├── modifications.txt
    └── original-file.zip
```

## 14.2 前台素材信息

汽车详情页底部提供“素材信息”折叠区域。

内容包括：

- 素材名称；
- 作者；
- 来源；
- 许可证；
- 修改说明；
- 展示说明。

示例：

```text
模型名称：Low Poly Sports Car
作者：作者名称
来源：原始资源详情页
许可证：CC BY 4.0
修改说明：已进行减面、材质调整和 GLB 格式转换
展示说明：通用跑车模型，不代表具体品牌或量产车型
```

## 14.3 全站素材页

提供“素材与感谢”页面，集中展示：

- 3D 模型作者；
- 图片作者；
- 音频作者；
- 许可证；
- 修改说明；
- 下架联系邮箱。

---

# 15. 验收标准

## 15.1 核心流程

用户可以完成：

```text
进入首页
→ 选择汽车分类
→ 选择一辆汽车
→ 进入详情页
→ 加载 3D 模型
→ 旋转和缩放
→ 切换视角
→ 切换颜色
→ 收藏汽车
```

全流程不得要求登录。

## 15.2 3D 验收

必须满足：

- 鼠标可以旋转；
- 触摸可以旋转；
- 鼠标滚轮可以缩放；
- 双指可以缩放；
- 可以恢复默认视角；
- 车辆不会意外翻转；
- 相机不会进入模型内部；
- 模型失败时可以重试；
- 模型失败后可以查看图片；
- 切换颜色不重新加载页面；
- 页面离开后资源可以被释放。

## 15.3 内容验收

必须满足：

- 管理员无需修改代码即可新增汽车；
- 草稿内容不能被普通用户访问；
- 每份第三方素材有来源记录；
- 授权信息不完整时不能发布；
- 通用模型不能被描述为真实车型；
- 没有授权依据的车标不能发布。

## 15.4 儿童体验验收

邀请至少 3 名儿童和 3 名家长测试。

观察：

- 儿童是否能独立进入汽车详情；
- 儿童是否能独立旋转汽车；
- 儿童是否理解颜色按钮；
- 是否频繁误触；
- 家长是否能找到声音设置；
- 家长是否认可内容安全性。

---

# 16. 开发优先级

## 16.1 P0：MVP 必须完成

- 响应式首页；
- 汽车分类；
- 汽车列表；
- 汽车详情；
- GLB 模型加载；
- 旋转；
- 缩放；
- 复位；
- 预设视角；
- 加载进度；
- 加载失败降级；
- 颜色切换；
- 本地收藏；
- 最近浏览；
- 基础品牌卡；
- 素材信息展示；
- 车辆名称与品牌名称语音（至少覆盖多数已发布车辆，缺口可静音降级，见 6.7.3）；
- 分类、品牌和汽车后台管理；
- 素材许可证管理；
- 发布状态管理。

## 16.2 P1：建议完成

- 部件热点；
- 部件名称语音；
- 自动旋转；
- 全屏模式；
- 搜索；
- 使用时间提醒；
- 高清和低清模型切换；
- 基础访问统计。

## 16.3 P2：后续版本

- 语音搜索；
- 汽车知识小游戏；
- 车辆声音；
- 内饰展示；
- 车门和车灯动画；
- AR；
- 家长账号；
- 多设备收藏同步；
- 中英文切换；
- 特种车辆专题。

---

# 17. 项目实施计划

## 阶段一：技术验证

目标：

- 找到 1～3 个授权明确的免费 GLB 模型；
- 完成单车 3D Demo；
- 验证手机、平板和电脑操作；
- 验证颜色切换；
- 验证模型压缩；
- 验证署名展示。

完成标准：

> 普通手机可以正常加载一辆汽车，并完成旋转、缩放和复位。

## 阶段二：基础产品开发

开发：

- 首页；
- 分类；
- 汽车列表；
- 汽车详情；
- 3D 查看器；
- 收藏；
- 最近浏览；
- 品牌卡；
- 素材信息页面。

## 阶段三：后台开发

开发：

- 管理员登录；
- 分类管理；
- 品牌管理；
- 汽车管理；
- 文件上传；
- 模型预览；
- 许可证管理；
- 发布校验。

## 阶段四：内容制作

完成：

- 12～20 个免费模型的审核和优化；
- 封面图生成；
- 儿童文案；
- 名称语音；
- 许可证存档；
- 多设备测试。

## 阶段五：灰度上线

先供家庭内部或少量家庭使用。

收集：

- 加载速度；
- 加载失败率；
- 儿童操作情况；
- 家长反馈；
- 最受欢迎的汽车分类。

---

# 18. AI 开发执行规则

AI 编程工具必须遵循以下规则：

1. 优先完成 P0，不主动开发 P2；
2. 所有代码使用 TypeScript；
3. 组件、接口和数据类型必须明确；
4. 不在代码中写死品牌和车型数据；
5. 内容通过数据库或配置文件管理；
6. 模型必须按需加载；
7. Three.js 资源必须在组件卸载时释放；
8. 所有异步请求必须包含加载、失败和空状态；
9. 所有图片必须包含替代文本；
10. 所有上传接口必须验证文件；
11. 所有管理接口必须认证；
12. 任何素材没有许可证记录时不得发布；
13. 通用模型不得标记为真实车型；
14. 不添加广告、支付、用户评论和儿童账号；
15. 每完成一个模块，需要同时提供测试；
16. 不得通过代码自动抓取汽车官网图片和模型；
17. 不得使用来源不明的演示素材作为正式数据；
18. 本地开发允许使用明确标记为占位资源的几何模型。

---

# 19. 建议项目目录

```text
src/
├── app/
│   ├── page.tsx
│   ├── cars/
│   │   ├── page.tsx
│   │   └── [slug]/page.tsx
│   ├── categories/
│   │   └── [slug]/page.tsx
│   ├── brands/
│   │   ├── page.tsx
│   │   └── [slug]/page.tsx
│   ├── favorites/page.tsx
│   ├── history/page.tsx
│   ├── settings/page.tsx
│   ├── credits/page.tsx
│   ├── admin/
│   └── api/v1/
├── components/
│   ├── car/
│   ├── brand/
│   ├── layout/
│   ├── three/
│   │   ├── CarViewer.tsx
│   │   ├── CameraControls.tsx
│   │   ├── ColorSelector.tsx
│   │   ├── HotspotLayer.tsx
│   │   └── ModelLoader.tsx
│   └── ui/
├── lib/
│   ├── db/
│   ├── storage/
│   ├── validation/
│   ├── three/
│   └── licenses/
├── hooks/
├── stores/
├── types/
├── styles/
└── tests/

prisma/
├── schema.prisma
└── seed.ts

public/
├── images/
├── audio/
└── placeholders/

assets-license/
└── README.md
```

---

# 20. Definition of Done

一个功能只有满足以下条件才算完成：

- 功能符合需求；
- 桌面端和移动端可用；
- 有加载状态；
- 有失败状态；
- 有空数据状态；
- TypeScript 无类型错误；
- ESLint 检查通过；
- 核心逻辑有测试；
- 没有明显控制台错误；
- 不泄露密钥；
- 不使用来源不明的正式素材；
- 对应文档已经更新。

---

# 21. 首个开发任务

首个开发任务不是制作全部页面，而是完成独立的 3D 汽车查看器原型。

必须实现：

1. 加载一个本地 GLB 模型；
2. 显示加载进度；
3. 鼠标拖动旋转；
4. 单指拖动旋转；
5. 滚轮和双指缩放；
6. 一键复位；
7. 前、后、左、右、上方视角；
8. 自动旋转开关；
9. 模型加载失败提示；
10. 图片降级；
11. 组件卸载时释放模型资源；
12. 在普通手机上测试性能。

原型通过后，再开始开发完整网站。

---

# 22. 项目最终原则

项目始终围绕以下核心价值：

> 让孩子能够简单、安全、有趣地观察汽车、认识汽车。

实施过程中坚持：

- 先通用汽车，后真实品牌；
- 先核心 3D 体验，后扩展功能；
- 先保证合法来源，后增加内容数量；
- 先保证手机流畅，后追求超高清效果；
- 不用通用模型冒充真实车型；
- 不因追求品牌数量而使用来源不明的资源。
---

# 23. 页面路由清单

| 页面 | 路由 | 权限 | 优先级 |
|---|---|---|---|
| 首页 | `/` | 公开 | P0 |
| 汽车列表 | `/cars` | 公开 | P0 |
| 汽车详情 | `/cars/[slug]` | 公开 | P0 |
| 分类详情 | `/categories/[slug]` | 公开 | P0 |
| 品牌列表 | `/brands` | 公开 | P0 |
| 品牌详情 | `/brands/[slug]` | 公开 | P1 |
| 搜索结果 | `/search` | 公开 | P1 |
| 我的收藏 | `/favorites` | 公开、本地数据 | P0 |
| 最近浏览 | `/history` | 公开、本地数据 | P0 |
| 家长设置 | `/settings` | 公开 | P1 |
| 素材与感谢 | `/credits` | 公开 | P0 |
| 隐私说明 | `/privacy` | 公开 | P0 |
| 管理员登录 | `/admin/login` | 未登录管理员 | P0 |
| 管理首页 | `/admin` | 管理员 | P0 |
| 分类管理 | `/admin/categories` | 管理员 | P0 |
| 品牌管理 | `/admin/brands` | 管理员 | P0 |
| 汽车管理 | `/admin/vehicles` | 管理员 | P0 |
| 汽车编辑 | `/admin/vehicles/[id]` | 管理员 | P0 |
| 素材管理 | `/admin/assets` | 管理员 | P0 |
| 许可证管理 | `/admin/licenses` | 管理员 | P0 |

---

# 24. 页面详细状态

所有读取数据的页面必须实现以下状态：

## 24.1 加载状态

- 显示骨架屏；
- 不展示空白页面；
- 不阻塞基础导航；
- 3D 模型加载与页面数据加载相互独立。

## 24.2 空数据状态

示例文案：

```text
这里还没有汽车，稍后再来看看吧。
```

空状态可以提供：

- 返回首页；
- 查看热门汽车；
- 随机看一辆。

## 24.3 请求失败状态

示例文案：

```text
汽车暂时没有开过来，请再试一次。
```

必须提供：

- 重试按钮；
- 返回上一页；
- 错误日志上报。

## 24.4 内容不存在状态

当 slug 不存在、内容未发布或已下架时：

- 返回 HTTP 404；
- 展示儿童友好的 404 页面；
- 不泄露后台数据；
- 推荐其他已发布汽车。

---

# 25. 3D 查看器组件规格

## 25.1 组件接口

```typescript
interface CarViewerProps {
  vehicleId: string;
  highModelUrl?: string;
  lowModelUrl?: string;
  fallbackImageUrls: string[];
  coverImageUrl: string;
  defaultCamera: CameraConfig;
  colors: VehicleColor[];
  hotspots: Hotspot[];
  autoRotate?: boolean;
  preferredQuality?: "AUTO" | "HIGH" | "LOW";
  onLoadStart?: () => void;
  onProgress?: (progress: number) => void;
  onLoadSuccess?: () => void;
  onLoadError?: (error: Error) => void;
  onInteraction?: (type: ViewerInteractionType) => void;
}
```

## 25.2 交互事件类型

```typescript
type ViewerInteractionType =
  | "ROTATE"
  | "ZOOM"
  | "RESET"
  | "VIEW_FRONT"
  | "VIEW_BACK"
  | "VIEW_LEFT"
  | "VIEW_RIGHT"
  | "VIEW_TOP"
  | "COLOR_CHANGE"
  | "HOTSPOT_CLICK"
  | "FULLSCREEN_ENTER"
  | "FULLSCREEN_EXIT"
  | "AUTO_ROTATE_START"
  | "AUTO_ROTATE_STOP";
```

## 25.3 查看器内部状态

```typescript
type ViewerStatus =
  | "IDLE"
  | "LOADING"
  | "READY"
  | "ERROR"
  | "FALLBACK_IMAGE";
```

需要维护：

- 当前加载进度；
- 当前模型质量；
- 当前车身颜色；
- 当前相机位置；
- 当前选中的热点；
- 是否自动旋转；
- 是否全屏；
- 是否发生加载错误。

## 25.4 相机控制规则

- 使用透视相机；
- 默认 FOV 建议为 35～50；后台可按车辆通过 `CameraConfig.fov` 覆盖（见 8.4）；
- 相机观察目标默认指向车身中心；
- 禁止相机平移，避免儿童把汽车拖出画面；
- 开启阻尼，保证旋转平滑；
- 对旋转速度和缩放速度设置上限；
- 切换预设视角时使用 300～600ms 平滑动画；
- 用户主动操作时中止相机切换动画。

## 25.5 模型加载规则

加载优先级：

```text
AUTO 模式：
设备性能良好且网络较快 → 高清模型
其他情况 → 低清模型
```

出现以下情况时优先低清模型：

- 移动设备；
- `navigator.connection.saveData` 为 true；
- 网络类型为 2G 或 3G；
- 设备内存较低；
- 高清模型曾加载失败。

不得仅通过 User-Agent 判断设备性能。

## 25.6 资源释放规则

组件卸载或切换模型时必须释放：

- Geometry；
- Material；
- Texture；
- Environment Map；
- Render Target；
- Animation Mixer；
- Event Listener；
- requestAnimationFrame；
- ResizeObserver。

释放后移除场景中的旧模型引用，避免多次浏览后内存持续增加。

---

# 26. 本地数据存储

## 26.1 LocalStorage Key

统一使用命名空间：

```text
little-car-museum:favorites
little-car-museum:history
little-car-museum:settings
little-car-museum:schema-version
```

## 26.2 收藏结构

```typescript
interface FavoriteRecord {
  vehicleId: string;
  createdAt: string;
}
```

最多保存 100 条收藏。

## 26.3 最近浏览结构

```typescript
interface HistoryRecord {
  vehicleId: string;
  viewedAt: string;
}
```

规则：

- 最多保存 20 条；
- 相同汽车只保留最新记录；
- 最新记录排在最前；
- 汽车已下架时不在前台展示。

> 收藏与最近浏览都只保存 `vehicleId`，无服务端身份。内容变更（下架 / 删除 / 改名 / 换 slug）后的判断以**前台接口返回为准**，见 26.6。

## 26.4 设置结构

```typescript
interface LocalSettings {
  soundEnabled: boolean;
  autoRotateEnabled: boolean;
  preferredQuality: "AUTO" | "HIGH" | "LOW";
  usageReminderMinutes: 0 | 15 | 30 | 45;
}
```

## 26.5 存储异常处理

如果 LocalStorage：

- 不可用；
- 数据损坏；
- JSON 解析失败；
- 数据版本不兼容；

系统应恢复默认值，不能导致页面崩溃。

## 26.6 内容变更后的失效策略

收藏与最近浏览均保存在本地，内容侧（服务端）无法主动清除用户浏览器数据。因此统一以**前台接口返回值作为唯一事实来源**：

- 前台收藏页 / 最近浏览页渲染时，向服务端校验本地 `vehicleId` 列表，仅展示当前可访问（已发布）且仍存在的汽车；
- 对已下架、已删除或返回 404 的 `vehicleId`，本地记录应**在本次渲染时被剔除并回写**，避免其在以后“占位”；
- 汽车改名不影响 `vehicleId` 关联，重命名后前台按最新名称展示；换 slug 不影响详情接口按 id 访问时的展示（若接口仅支持 slug，则在校验阶段用 id→slug 映射兜底）；
- 收藏上限 100 / 最近浏览上限 20 按**清洗后的有效记录**计算；
- 数据版本（`schema-version`）升级时，对不兼容的旧结构执行一次迁移或整体重置，见 26.5。

> 说明：MVP 本地收藏是尽力而为的体验，若无法在线校验（离线或接口失败），应保留本地展示并标记“内容可能已更新”，不得因校验失败而清空用户收藏。

---

# 27. 数据库约束

## 27.1 唯一约束

以下字段必须唯一：

- `Category.slug`；
- `Brand.slug`；
- `Vehicle.slug`；
- 管理员邮箱。

## 27.2 删除规则

### 分类删除

分类下存在汽车时，不允许直接删除。

管理员可以：

- 先迁移汽车；
- 停用分类；
- 确认无关联数据后删除。

### 品牌删除

品牌下存在汽车时：

- 默认禁止删除；
- 可以将品牌设为停用；
- 通用汽车允许不关联品牌。

### 汽车删除

默认采用软删除或归档，不直接删除数据库记录。

删除汽车时：

- 前台不可访问；
- 收藏记录自然失效；
- 模型文件不能立即物理删除；
- 必须确认没有其他内容引用相同素材。

### 许可证删除

只要许可证仍被任何素材引用，就不允许删除。

---

# 28. 素材发布业务规则

## 28.1 通用发布条件

汽车内容发布前必须满足：

- 名称不为空；
- slug 合法且唯一；
- 至少关联一个分类；
- 儿童介绍不为空；
- 封面图存在；
- 展示类型明确；
- 所有素材通过许可证审核；
- 至少存在一种可用展示资源。

## 28.2 REAL_3D 发布条件

除通用条件外，还必须满足：

- `isRealModel = true`；
- 必须关联真实品牌；
- 车型名称与模型相符；
- 真实车型依据已经人工核对；
- 3D 模型许可证允许公开展示；
- 修改过模型时许可证允许修改；
- 页面存在非官方声明。

## 28.3 GENERIC_3D 发布条件

必须满足：

- `isRealModel = false`；
- 不得使用真实量产车型名称；
- 不得暗示获得品牌官方授权；
- 页面展示通用模型声明。

## 28.4 IMAGE_SET 发布条件

必须至少有：

- 1 张封面图（同时作为卡片缩略图）；
- **3 张不同角度的图片（硬性条件，建议 4~6 张）**：至少包含前、侧、后三个方向，用于替代 3D 的多角度观察；
- 每张图片都有许可证记录。

> 说明：`IMAGE_SET` 的多角度图同时也是 3D 加载失败时的降级资源（见 FR-3D-009 的“多角度图片”档位），因此将其与降级链共用一份已验证的图片集合。若某车仅能提供单张图片，应使用 `STATIC_IMAGE` 而非 `IMAGE_SET`。

## 28.5 STATIC_IMAGE 发布条件

必须至少有：

- 1 张可公开使用的图片；
- 图片替代文本；
- 图片许可证记录。

---

# 29. 文件上传规则

## 29.1 允许的文件类型

### 3D 模型

```text
.glb
```

MVP 不直接接受：

```text
.obj
.fbx
.blend
.gltf + 外部资源
```

以上格式应在线下处理并转换成单个 GLB 后再上传。

### 图片

```text
.jpg
.jpeg
.png
.webp
.avif
```

### 音频

```text
.mp3
.m4a
.ogg
.wav
```

许可证截图和文件可以使用：

```text
.png
.jpg
.pdf
.txt
```

## 29.2 文件大小限制

建议默认值：

| 文件 | 最大大小 |
|---|---:|
| 低清 GLB | 20MB |
| 高清 GLB | 50MB |
| 普通图片 | 5MB |
| 音频 | 10MB |
| 许可证附件 | 10MB |

限制应通过环境变量配置。

## 29.3 安全要求

上传时必须：

- 验证扩展名；
- 验证 MIME 类型；
- 检查文件头；
- 使用随机对象存储路径；
- 禁止执行上传文件；
- 清理原始文件名中的特殊字符；
- 记录上传管理员和上传时间；
- 限制管理员上传频率。

---

# 30. SEO 与公开访问规则

## 30.1 页面元数据

公开页面需要配置：

- 页面标题；
- 页面描述；
- Open Graph 图片；
- Canonical URL。

汽车详情页标题示例：

```text
红色闪电跑车 3D 展示｜小小汽车馆
```

## 30.2 搜索引擎控制

以下页面禁止收录：

```text
/admin/*
/api/*
/settings
/history
/favorites
```

草稿、待审核和已下架内容不得出现在 Sitemap 中。

## 30.3 非官方声明

品牌和真实车型页面统一展示：

> 本网站是非官方的儿童汽车认知项目，与页面提到的汽车品牌不存在合作、授权或赞助关系，除非页面另有明确说明。

---

# 31. 统计事件

统计必须匿名，不采集儿童身份信息。

## 31.1 基础事件

```typescript
type AnalyticsEvent =
  | "PAGE_VIEW"
  | "VEHICLE_VIEW"
  | "MODEL_LOAD_START"
  | "MODEL_LOAD_SUCCESS"
  | "MODEL_LOAD_FAILED"
  | "MODEL_FALLBACK_USED"
  | "VEHICLE_ROTATED"
  | "VEHICLE_ZOOMED"
  | "VIEW_PRESET_USED"
  | "COLOR_CHANGED"
  | "HOTSPOT_CLICKED"
  | "FAVORITE_ADDED"
  | "FAVORITE_REMOVED"
  | "SEARCH_PERFORMED"
  | "SEARCH_NO_RESULT";
```

## 31.2 禁止采集的数据

不得采集：

- 儿童姓名；
- 输入设备指纹；
- 精确位置；
- 麦克风数据；
- 摄像头数据；
- 跨网站追踪标识；
- 与产品目标无关的个人信息。

## 31.3 统计去重

旋转和缩放属于高频事件。

每次汽车详情访问中：

- 首次旋转记录一次；
- 首次缩放记录一次；
- 不记录每一帧操作。

## 31.4 错误上报与匿名化

前台错误上报（如 24.3 的“错误日志上报”）同样遵守本节的匿名原则：

- 上报内容只包含：错误类型与代码、出现错误的页面路径（去掉查询参数）、浏览器能力标记（是否支持 WebGL、约略网络提示）；不包含 URL 中的可识别参数、本地收藏内容或输入文本；
- 错误上报在**儿童访问区域默认关闭**，仅在家长设置中确认后允许；后台（管理员）区域可独立开启；
- 若选用第三方错误监控（如 Sentry 等），必须在儿童区域禁用其默认的跨站/设备指纹与全局用户上下文采集，并确认其未向儿童暴露隐私许可（consent）弹窗；否则 MVP 不在前台使用该类工具，仅使用自建/本地匿名日志；
- 上报按 31.2 禁止采集的清单同样适用，不采集儿童姓名、设备指纹、精确位置等。

---

# 32. 错误码约定

API 统一返回：

```typescript
interface ApiResponse<T> {
  success: boolean;
  data?: T;
  error?: {
    code: string;
    message: string;
    requestId?: string;
  };
}
```

常用错误码：

| 错误码 | 含义 |
|---|---|
| `VALIDATION_ERROR` | 请求参数错误 |
| `UNAUTHORIZED` | 未登录 |
| `FORBIDDEN` | 权限不足 |
| `NOT_FOUND` | 内容不存在 |
| `SLUG_CONFLICT` | slug 重复 |
| `ASSET_LICENSE_REQUIRED` | 缺少许可证 |
| `ASSET_LICENSE_REJECTED` | 许可证未通过 |
| `PUBLISH_CHECK_FAILED` | 发布检查失败 |
| `FILE_TYPE_NOT_ALLOWED` | 文件类型不允许 |
| `FILE_TOO_LARGE` | 文件过大 |
| `MODEL_PROCESS_FAILED` | 模型处理失败 |
| `INTERNAL_ERROR` | 服务端错误 |

前台面向儿童的错误提示不得直接显示技术堆栈。

---

# 33. 环境变量

示例：

```env
DATABASE_URL=
AUTH_SECRET=
NEXT_PUBLIC_SITE_URL=http://localhost:3000

STORAGE_ENDPOINT=
STORAGE_REGION=
STORAGE_BUCKET=
STORAGE_ACCESS_KEY=
STORAGE_SECRET_KEY=
STORAGE_PUBLIC_BASE_URL=

MAX_HIGH_MODEL_SIZE_MB=50
MAX_LOW_MODEL_SIZE_MB=20
MAX_IMAGE_SIZE_MB=5
MAX_AUDIO_SIZE_MB=10

NEXT_PUBLIC_ANALYTICS_ENABLED=false
ERROR_MONITORING_DSN=
```

要求：

- 提供 `.env.example`；
- 不提交真实 `.env`；
- 服务端密钥不得使用 `NEXT_PUBLIC_` 前缀；
- 日志中不得输出密钥。

---

# 34. 测试要求

> 测试分级原则：MVP 必须保证本次需求的“核心逻辑”通过即可（见下方各分级），优先覆盖 P0 功能与素材发布校验相关的风险点；P1/P2 功能测试可随模块完成补充，不要求在上线前全部铺开，避免测试范围过大拖慢 MVP。

## 34.1 单元测试

至少覆盖：

- 收藏添加和删除；
- 最近浏览去重；
- LocalStorage 异常恢复；
- 发布条件校验；
- 许可证条件校验；
- slug 生成；
- 搜索参数解析；
- 模型质量选择；
- 文件类型和大小校验。

## 34.2 组件测试

至少覆盖：

- 汽车卡片；
- 分类筛选；
- 收藏按钮；
- 颜色选择器；
- 加载进度；
- 3D 加载失败状态；
- 图片降级状态；
- 素材署名区域；
- 管理后台发布按钮。

## 34.3 端到端测试

核心用例：

### E2E-001 浏览汽车

```text
进入首页
→ 点击分类
→ 点击汽车
→ 看到汽车详情
→ 模型加载完成
```

### E2E-002 收藏汽车

```text
进入汽车详情
→ 点击收藏
→ 进入收藏页面
→ 看到该汽车
→ 刷新页面
→ 收藏仍然存在
```

### E2E-003 模型加载失败

```text
模拟模型请求失败
→ 展示失败提示
→ 点击查看图片版
→ 显示降级图片
```

### E2E-004 后台发布校验

```text
创建汽车
→ 不填写许可证
→ 点击发布
→ 发布失败
→ 显示缺失字段
```

### E2E-005 正常发布

```text
创建汽车
→ 填写基础内容
→ 关联已批准许可证
→ 上传展示资源
→ 点击发布
→ 前台可以访问
```

## 34.4 浏览器测试

至少测试：

- Chrome；
- Edge；
- Safari；
- iOS Safari；
- Android Chrome。

## 34.5 设备测试

至少选择：

- 一台普通 Windows 或 macOS 电脑；
- 一台 iPhone 或 iPad；
- 一台中端 Android 手机；
- 一台性能较弱的移动设备。

---

# 35. AI 开发任务拆分

AI 必须按以下顺序实施，不得跳过核心基础设施。

## TASK-001 初始化项目

完成：

- 创建 Next.js TypeScript 项目；
- 配置 Tailwind CSS；
- 配置 ESLint 和 Prettier；
- 配置 Vitest；
- 配置 Playwright；
- 建立推荐目录；
- 提供 `.env.example`；
- 提供 README。

验收：

```bash
npm run dev
npm run lint
npm run typecheck
npm run test
npm run build
```

全部成功。

## TASK-002 建立数据类型和模拟数据

完成：

- 创建所有核心 TypeScript 类型；
- 创建 3～5 条通用汽车模拟数据；
- 模拟资源必须标记为占位数据；
- 不使用真实品牌图片。

## TASK-003 开发汽车列表

完成：

- 汽车卡片；
- 分类筛选；
- 空状态；
- 加载状态；
- 失败状态；
- 响应式布局。

## TASK-004 开发 3D 查看器原型

完成：

- GLB 加载；
- 加载进度；
- 旋转；
- 缩放；
- 复位；
- 预设视角；
- 自动旋转；
- 图片降级；
- 资源释放。

允许使用自行创建的基础几何体作为开发占位模型。

## TASK-005 开发汽车详情

完成：

- 3D 查看器集成；
- 汽车名称和介绍；
- 颜色切换；
- 收藏；
- 热点；
- 素材信息；
- 通用模型声明。

## TASK-006 开发本地收藏和最近浏览

完成：

- Zustand 或独立 Hooks；
- LocalStorage 持久化；
- 数据版本；
- 异常恢复；
- 收藏和历史页面。

## TASK-007 建立数据库

完成：

- PostgreSQL；
- Prisma；
- 数据表；
- 数据库迁移；
- Seed 数据；
- 唯一约束和关联关系。

## TASK-008 开发公共 API

完成：

- 分类接口；
- 品牌接口；
- 汽车列表接口；
- 汽车详情接口；
- 随机汽车接口；
- 搜索接口；
- 统一错误结构。

## TASK-009 开发管理员认证

完成：

- 管理员登录；
- Session；
- 路由保护；
- 登录失败限流；
- 退出登录；
- 不提供公众注册。

## TASK-010 开发后台管理

完成：

- 分类 CRUD；
- 品牌 CRUD；
- 汽车 CRUD；
- 许可证 CRUD；
- 发布和下架；
- 发布校验；
- 表单错误提示。

## TASK-011 开发文件上传

完成：

- 图片上传；
- GLB 上传；
- 音频上传；
- 文件校验；
- 对象存储；
- 上传进度；
- 上传失败处理。

## TASK-012 性能与兼容优化

完成：

- 模型按需加载；
- 高清和低清选择；
- 图片优化；
- WebGL 检测；
- 资源释放验证；
- 移动端性能测试。

## TASK-013 完整测试与发布

完成：

- 单元测试；
- 组件测试；
- E2E；
- 可访问性检查；
- 安全检查；
- 构建检查；
- 部署文档；
- 备份和恢复说明。

---

# 36. AI 每个任务的输出格式

AI 完成每个任务后必须输出：

```text
1. 本次完成内容
2. 新增或修改的文件
3. 关键技术决策
4. 数据库或环境变量变化
5. 测试内容和结果
6. 手动验证步骤
7. 当前遗留问题
8. 下一步建议
```

AI 不得只返回代码片段而不说明文件路径。

修改已有文件时，应给出：

- 完整文件内容；或
- 明确可应用的 diff。

---

# 37. 部署要求

## 37.1 开发环境

支持：

```bash
npm install
npm run dev
```

数据库可以使用：

- 本地 PostgreSQL；或
- Docker Compose。

## 37.2 生产环境

需要支持：

- Node.js 运行环境；
- PostgreSQL；
- S3 兼容对象存储；
- HTTPS；
- CDN；
- 定时数据库备份。

## 37.3 Docker Compose

建议提供：

- Web 服务；
- PostgreSQL；
- 可选的 MinIO 本地对象存储。

要求：

- 通过 `.env` 注入变量，不得将管理员密码和数据库密码写死在 Compose 文件中；
- Compose 中数据库、MinIO 的账号密码一律引用 `.env`，并随 `.env.example` 提供占位；
- 提供首启初始化说明（启动数据库 → 运行迁移 → 创建管理员 → 初始化 MinIO 存储桶并关闭匿名写）；

## 37.4 发布前检查

```text
[ ] 环境变量已配置
[ ] 数据库迁移成功
[ ] 管理员账号已创建
[ ] HTTPS 已开启
[ ] 对象存储禁止匿名写入
[ ] 后台路由受保护
[ ] 测试全部通过
[ ] Sitemap 不包含后台页面
[ ] 素材许可证已审核
[ ] 隐私说明已发布
[ ] 版权联系邮箱可用
[ ] 模型加载失败降级可用
[ ] 手机端已完成真机测试
```

---

# 38. 内容录入模板

## 38.1 通用汽车模板

```yaml
nameCn: 红色闪电跑车
nameEn: Red Lightning Sports Car
childName: 红色闪电
displayType: GENERIC_3D
isRealModel: false
categories:
  - sports-car
energyType: OTHER
seatCount: 2
childDescription: 这是一辆车身低低的跑车，看起来像准备向前冲。
coverImageUrl: ""
highModelUrl: ""
lowModelUrl: ""
fallbackImageUrls: []
isHot: false
status: DRAFT
```

## 38.2 素材许可证模板

```yaml
assetName: ""
assetType: MODEL
authorName: ""
sourcePageUrl: ""
originalDownloadUrl: ""
licenseName: CC BY 4.0
licenseUrl: https://creativecommons.org/licenses/by/4.0/
acquiredAt: YYYY-MM-DD
allowPublicDisplay: true
allowModification: true
allowCommercialUse: true
attributionRequired: true
attributionText: ""
containsTrademark: false
modifications: 已转换为 GLB，并进行了减面和纹理压缩。
reviewStatus: PENDING
```

## 38.3 发布审核清单

```text
[ ] 名称适合儿童
[ ] 描述简短、准确
[ ] 未将通用模型描述为真实车型
[ ] 封面图片正常
[ ] 低清模型正常
[ ] 高清模型正常
[ ] 手机端可流畅操作
[ ] 模型来源明确
[ ] 许可证允许网页展示
[ ] 许可证允许当前修改
[ ] 作者署名正确
[ ] 贴图许可证已核对
[ ] 商标情况已登记
[ ] 降级图片可用
[ ] 素材信息页面可访问
```

---

# 39. 开发启动提示词

将本需求文档交给 AI 编程工具后，可使用以下提示词开始开发：

```text
请完整阅读《需求文档.md》，严格按照其中的优先级和 AI 开发执行规则实施项目。

当前只执行 TASK-001：初始化项目。

要求：
1. 使用 Next.js、React、TypeScript 和 Tailwind CSS；
2. 配置 ESLint、Prettier、Vitest 和 Playwright；
3. 建立需求文档指定的目录结构；
4. 提供 .env.example 和 README.md；
5. 暂时不要开发业务页面；
6. 不使用来源不明的图片或 3D 模型；
7. 完成后按照“AI 每个任务的输出格式”汇报；
8. 确保 lint、typecheck、test 和 build 可以通过。
```

完成 TASK-001 后，再逐项要求 AI 执行后续任务，不建议一次性让 AI 完成整个项目。

---

# 40. 需求变更原则

项目开发过程中，如提出新需求，必须先判断：

1. 是否服务于儿童观察和认识汽车；
2. 是否属于当前 P0；
3. 是否增加版权风险；
4. 是否显著增加移动端性能压力；
5. 是否涉及儿童个人信息；
6. 是否影响当前开发里程碑。

如果新需求不属于 P0，应进入后续需求池，不得影响 MVP 上线。

---

# 41. MVP 上线判定

只有同时满足以下条件，MVP 才允许上线：

- 至少有 12 辆可展示汽车（与 4.1 口径一致：含 3D 与图片降级车辆内容）；
- 至少有 6 个汽车分类拥有已发布内容并开放入口（4.1 目标为 8~10 个，本项为硬性下限）；
- 至少 8 辆汽车支持 3D 展示；
- 所有已发布素材授权信息完整；
- 所有 3D 页面都有图片降级方案；
- 核心流程 E2E 测试通过；
- 中端手机操作帧率基本达到 30 FPS；
- 模型加载成功率达到目标；
- 网站不含广告、支付和儿童公开账号；
- 隐私说明和素材感谢页面已经发布；
- 管理后台受到身份认证保护；
- 已完成一次数据库备份和恢复验证。

---

# 42. 最终产品边界声明

本项目是儿童汽车认知工具，不是汽车品牌官方网站、汽车销售平台或专业车辆数据库。

产品必须始终明确区分：

- 真实车型和通用模型；
- 官方颜色和展示颜色；
- 真实数据和儿童化介绍；
- 网站自身内容和第三方开放素材。

任何不确定来源、不确定许可证或疑似侵权的素材，都应保持未发布状态，直到完成核对。
